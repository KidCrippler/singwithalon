# SingWithAlon

The code behind Alon Cohen's sing-along business.

This repository holds two independent projects. See each project's own
`CLAUDE.md` / `README.md` for details:

- `react-app/` — the public website
- `platform/` — the sing-along app
