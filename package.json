{
  "name": "singwithalon",
  "private": true,
  "scripts": {
    "dev": "concurrently --names \"backend,frontend\" --prefix-colors \"blue,magenta\" \"npm --prefix platform/backend run dev\" \"npm --prefix platform/frontend run dev\"",
    "install:all": "npm --prefix platform/backend install && npm --prefix platform/frontend install"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
